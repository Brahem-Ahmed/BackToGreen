<!DOCTYPE html>

<title>Edit EvenementEcologique</title>

{% block body %}
    <h1>Edit EvenementEcologique</h1>

    {{ include('evenement_ecologique/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_evenement_ecologique_index') }}">back to list</a>

    {{ include('evenement_ecologique/_delete_form.html.twig') }}
{% endblock %}
