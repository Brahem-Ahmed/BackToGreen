<!DOCTYPE html>

<title>EvenementEcologique</title>

{% block body %}
    <h1>EvenementEcologique</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ evenement_ecologique.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ evenement_ecologique.titre }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ evenement_ecologique.description }}</td>
            </tr>
            <tr>
                <th>DateDebut</th>
                <td>{{ evenement_ecologique.dateDebut }}</td>
            </tr>
            <tr>
                <th>DateFin</th>
                <td>{{ evenement_ecologique.dateFin }}</td>
            </tr>
            <tr>
                <th>Lieu</th>
                <td>{{ evenement_ecologique.lieu }}</td>
            </tr>
            <tr>
                <th>CapaciteMax</th>
                <td>{{ evenement_ecologique.capaciteMax }}</td>
            </tr>
            <tr>
                <th>Categorie</th>
                <td>{{ evenement_ecologique.categorie }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_evenement_ecologique_index') }}">back to list</a>

    <a href="{{ path('app_evenement_ecologique_edit', {'id': evenement_ecologique.id}) }}">edit</a>

    {{ include('evenement_ecologique/_delete_form.html.twig') }}
{% endblock %}
