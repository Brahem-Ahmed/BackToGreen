<form method="post" action="{{ path('app_evenement_ecologique_delete', {'id': evenement_ecologique.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ evenement_ecologique.id) }}">
    <button class="btn">Delete</button>
</form>
