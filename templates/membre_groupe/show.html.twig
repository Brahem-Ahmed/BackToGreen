<!DOCTYPE html>

<title>MembreGroupe</title>

{% block body %}
    <h1>MembreGroupe</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ membre_groupe.id }}</td>
            </tr>
            <tr>
                <th>DateAdhesion</th>
                <td>{{ membre_groupe.dateAdhesion ? membre_groupe.dateAdhesion|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Statut</th>
                <td>{{ membre_groupe.statut }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_membre_groupe_index') }}">back to list</a>

    <a href="{{ path('app_membre_groupe_edit', {'id': membre_groupe.id}) }}">edit</a>

    {{ include('membre_groupe/_delete_form.html.twig') }}
{% endblock %}
