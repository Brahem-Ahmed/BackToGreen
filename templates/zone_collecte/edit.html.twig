<!DOCTYPE html>

<title>Edit ZoneCollecte</title>

{% block body %}
    <h1>Edit ZoneCollecte</h1>

    {{ include('zone_collecte/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_zone_collecte_index') }}">back to list</a>

    {{ include('zone_collecte/_delete_form.html.twig') }}
{% endblock %}
