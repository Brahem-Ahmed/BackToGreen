<!DOCTYPE html>

<title>CollecteDechet</title>

{% block body %}
    <h1>CollecteDechet</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ collecte_dechet.id }}</td>
            </tr>
            <tr>
                <th>DateCollecte</th>
                <td>{{ collecte_dechet.dateCollecte ? collecte_dechet.dateCollecte|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ collecte_dechet.quantite }}</td>
            </tr>
            <tr>
                <th>TypeDechet</th>
                <td>{{ collecte_dechet.typeDechet }}</td>
            </tr>
            <tr>
                <th>Statut</th>
                <td>{{ collecte_dechet.statut }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_collecte_dechet_index') }}">back to list</a>

    <a href="{{ path('app_collecte_dechet_edit', {'id': collecte_dechet.id}) }}">edit</a>

    {{ include('collecte_dechet/_delete_form.html.twig') }}
{% endblock %}
