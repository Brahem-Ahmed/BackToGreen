<form method="post" action="{{ path('app_collecte_dechet_delete', {'id': collecte_dechet.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ collecte_dechet.id) }}">
    <button class="btn">Delete</button>
</form>
